{
  "name": "study-session-os-extension",
  "version": "0.7.0",
  "private": true,
  "description": "A dependency-free Manifest V3 focus and study-capture extension.",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check popup.js && node --check blocked.js && node --check options.js && node --check scripts/chrome-smoke.mjs && node scripts/validate.mjs && node --test tests/background.test.mjs"
  }
}
